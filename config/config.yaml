system:
  name: "小跟班"
  log_level: "INFO"
  audio_sample_rate: 16000

wake_word:
  engine: "openwakeword"
  model_path: null  # null表示使用默认模型，自定义模型路径: "models/xiaoganban.onnx"
  threshold: 0.5

vad:
  engine: "silero"
  silence_duration_ms: 500  # 静音500ms判定结束
  max_recording_seconds: 10
  continuous_window_seconds: 3  # 连续对话窗口

stt:
  engine: "tencent"
  app_id: "${TENCENT_APP_ID}"
  secret_id: "${TENCENT_SECRET_ID}"
  secret_key: "${TENCENT_SECRET_KEY}"
  region: "ap-guangzhou"

llm:
  engine: "openai_compatible"
  api_base: "https://api.deepseek.com/v1"
  api_key: "${DEEPSEEK_API_KEY}"
  model: "deepseek-chat"
  temperature: 0.7
  max_tokens: 500
  stream: true
  system_prompt: |
    你是"小跟班"，腿哥的私人智能助理。
    你的性格特点：
    - 聪明机智，反应迅速
    - 略带幽默感，但不会过分开玩笑
    - 高效实用，注重解决问题

    回答要求：
    - 简洁明了，每句话控制在30字以内
    - 使用口语化表达，避免书面语
    - 如果不知道答案，诚实说明
    - 保持友好但专业的语气

tts:
  engine: "edge"
  voice: "zh-CN-YunxiNeural"  # 云希
  rate: "+0%"
  pitch: "+0Hz"

audio:
  input_device_index: 2  # UGREEN Camera 1080P 内置麦克风 (hw:3,0)
  output_device_index: 0  # bcm2835 Headphones 3.5mm耳机口
  chunk_size: 1024
  input_channels: 2  # UGREEN Camera 是双声道，read_chunk 会自动下混为单声道
